import styled from "styled-components";

const DialogActions = styled.div`
  align-items: center;
  display: flex;
  flex: 0 0 auto;
  justify-content: end;
  padding: 0.5rem;
`;

export default DialogActions;
